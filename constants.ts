
import { VulnerableMode } from './types';

export const VULNERABLE_MODES: VulnerableMode[] = [
    VulnerableMode.Infant,
    VulnerableMode.Pregnant,
    VulnerableMode.Pet,
    VulnerableMode.Respiratory,
];

export const GEMINI_MODEL = 'gemini-2.5-flash';
